apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8sdemo-api
  labels:
    app: k8sdemo-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: k8sdemo-api
  template:
    metadata:
      labels:
        app: k8sdemo-api
    spec:
      containers:
        - name: k8sdemo-api
          image: k8sdemo-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              value: "production"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /readyz
              port: 4000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 20
